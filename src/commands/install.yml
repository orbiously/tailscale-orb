description: >
  This command installs Tailscale
# What will this command do?
# Descriptions should be short, simple, and clear.
steps:
  - run:
      shell: bash
      name: Installing Tailscale
      command: <<include(scripts/install.sh)>>
